package Oak.Oak_Task.Scheduler_Agent is

    procedure Initialise_Agent
     (Agent               : access Oak_Task;
      Name                : in Task_Name;
      Call_Stack          : in Call_Stack_Handler;
      Max_Priority : in Priority;
      Min_Prioirty        : in Priority;
      Set_Up_Procedure    : in Address;
      Manage_Queues_Procedure : in Address;
      Add_Task_Procedure      : in Address;
     Remove_Task_Procedure          : in Address;
            Change_Task_Priority_Procedure : in Address );

   function Get_Desired_Wake_Time (Agent : in Oak_Task_Handler) return Time;
   function Get_Task_To_Run
     (Agent : in Oak_Task_Handler)
      return  Oak_Task_Handler;
   function Get_Manage_Queues_Address
     (Agent : in Oak_Task_Handler)
      return  Address;
   function Get_Add_Task_Address
     (Agent : in Oak_Task_Handler)
      return  Address;

   procedure Set_Run_Reason
     (Agent  : in Oak_Task_Handler;
      Reason : in Reason_For_Run);
   procedure Set_Priority_Range
     (Agent    : in Oak_Task_Handler;
      Min, Max : in Priority);
   procedure Set_Managed_Task
     (Agent : in Oak_Task_Handler;
      MT    : in Oak_Task_Handler);
end Oak.Oak_Task.Scheduler_Agent;

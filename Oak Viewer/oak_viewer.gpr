project Oak_Viewer is

   for Source_Dirs use ("src");
   for Object_Dir use "./obj";
   for Main use ("oak_viewer.adb");

   package Builder is
      for Default_Switches ("ada") use ("-g");
   end Builder;

   package Compiler is
      for Default_Switches ("ada") use ("-g");
   end Compiler;

   package Linker is
      for Default_Switches ("ada") use ("-g");
   end Linker;

end Oak_Viewer;

